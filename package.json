{
  "name": "runupto",
  "license": "MIT",
  "scripts": {
    "docker:proxy": "docker-compose up",
    "docker:api": "yarn --cwd ./runupto-api start:docker",
    "docker:webhook": "yarn --cwd ./runupto-webhook start:docker",
    "docker:auth": "yarn --cwd ./runupto-auth start:docker",
    "docker:redis": "yarn --cwd ./runupto-redis start:docker",
    "docker:mongo": "yarn --cwd ./runupto-mongo start:docker",
    "docker:queue": "yarn --cwd ./runupto-queue start:docker"
  }
}
